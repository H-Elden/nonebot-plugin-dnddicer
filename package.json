{
  "name": "nonebot-plugin-dnddicer-docs",
  "private": true,
  "type": "module",
  "description": "屠龙骰（DNDDicer）使用文档站——基于 VitePress 构建，仅用于文档开发与部署",
  "scripts": {
    "docs:sync-changelog": "node docs/.vitepress/scripts/sync-changelog.mjs",
    "docs:dev": "npm run docs:sync-changelog && vitepress dev docs",
    "docs:build": "npm run docs:sync-changelog && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
